name: Build LaTeX and Commit PDFs

on:
  push:
    branches: [ main ]
    paths-ignore:
      - 'Rules.pdf'
      - 'Challenges.pdf'
      - 'HSL-Fitness-Tests.pdf'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Compile Rules
        uses: xu-cheng/latex-action@v3
        with:
          root_file: Rules.tex

      - name: Compile Challenges
        uses: xu-cheng/latex-action@v3
        with:
          root_file: Challenges.tex

      - name: Compile HSL-Fitness-Tests
        uses: xu-cheng/latex-action@v3
        with:
          root_file: HSL-Fitness-Tests.tex

      - name: Commit PDFs
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if git diff --quiet -- Rules.pdf Challenges.pdf HSL-Fitness-Tests.pdf; then
            echo "No PDF changes"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Rules.pdf Challenges.pdf HSL-Fitness-Tests.pdf
          git commit -m "Auto-build PDFs"

          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git push origin HEAD:main